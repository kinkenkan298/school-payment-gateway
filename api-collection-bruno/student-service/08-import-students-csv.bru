meta {
  name: Import Students CSV
  type: http
  seq: 8
}

post {
  url: {{base_url_student}}/students/import
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file(/path/to/students.csv)
}
