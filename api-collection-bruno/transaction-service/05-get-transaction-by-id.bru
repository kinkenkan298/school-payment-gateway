meta {
  name: Get Transaction by ID
  type: http
  seq: 5
}

get {
  url: {{base_url_transaction}}/transactions/{{transaction_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
