meta {
  name: Get Payment Stats
  type: http
  seq: 5
}

get {
  url: {{base_url_payment}}/payments/stats?schoolId={{school_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
