meta {
  name: Reject KYC
  type: http
  seq: 4
}

post {
  url: {{base_url_admin}}/admin/schools/{{school_id}}/kyc/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "reason": "Dokumen NPSN tidak sesuai dengan data dinas pendidikan"
  }
}
