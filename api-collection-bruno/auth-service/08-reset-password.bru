meta {
  name: Reset Password
  type: http
  seq: 8
}

post {
  url: {{base_url_auth}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "PASTE_TOKEN_FROM_EMAIL_HERE",
    "password": "NewPassword123"
  }
}
