meta {
  name: Change Password
  type: http
  seq: 9
}

post {
  url: {{base_url_auth}}/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "currentPassword": "Password123",
    "newPassword": "NewPassword123"
  }
}
