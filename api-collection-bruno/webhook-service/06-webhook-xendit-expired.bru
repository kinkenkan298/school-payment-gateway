meta {
  name: [Webhook] Xendit - Expired (EXPIRED)
  type: http
  seq: 6
}

post {
  url: {{base_url_webhook}}/webhooks/xendit
  body: json
  auth: none
}

headers {
  x-callback-token: skip_in_dev
}

body:json {
  {
    "id": "XENDIT_INV_003",
    "external_id": "{{external_id}}",
    "status": "EXPIRED",
    "amount": 255000,
    "currency": "IDR"
  }
}
