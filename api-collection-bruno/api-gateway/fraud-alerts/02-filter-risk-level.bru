meta {
  name: [GW] Filter Alerts by Risk Level
  type: http
  seq: 2
}

get {
  url: {{base_url_gateway}}/api/v1/fraud-alerts?riskLevel=high&status=open
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
