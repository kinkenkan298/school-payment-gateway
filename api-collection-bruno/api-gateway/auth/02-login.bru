meta {
  name: [GW] Login
  type: http
  seq: 2
}

post {
  url: {{base_url_gateway}}/api/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "budi@sekolah.sch.id",
    "password": "Password123!"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("access_token", res.body.data.accessToken);
  }
}
