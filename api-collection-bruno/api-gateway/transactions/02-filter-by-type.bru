meta {
  name: [GW] Filter Transactions by Type
  type: http
  seq: 2
}

get {
  url: {{base_url_gateway}}/api/v1/transactions?type=payment&page=1&limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
