meta {
  name: [GW] Get All Payments
  type: http
  seq: 2
}

get {
  url: {{base_url_gateway}}/api/v1/payments?page=1&limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
