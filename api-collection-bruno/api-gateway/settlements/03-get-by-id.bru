meta {
  name: [GW] Get Settlement by ID
  type: http
  seq: 3
}

get {
  url: {{base_url_gateway}}/api/v1/settlements/{{settlement_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
